[package]
name = "nframework-core-cli-clap"
version = "0.1.0"
edition = "2024"

[dependencies]
nframework-core-cli-abstractions = { path = "../nframework-core-cli-abstractions" }
clap = { version = "4", features = ["std", "string"] }
